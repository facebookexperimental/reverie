# Codex skill entrypoints

Stock Codex discovers the LiteInst-specific Reverie skills here. Every entry is
a whole-package symlink to the canonical `.llms/skills/<name>/` package;
`.claude/skills` links to the same root.

Run the repository-root `scripts/check-skill-discovery.rs` after changes.
